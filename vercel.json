{
  "buildCommand": "cd 03_build/web && npm install && npm run build",
  "outputDirectory": "03_build/web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
